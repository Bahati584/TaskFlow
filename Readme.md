# TaskFlow Backend API

A complete task management backend built with Node.js, Express, and MySQL.

## Features

- ✅ Task CRUD operations
- ✅ User management
- ✅ Filtering and pagination
- ✅ Statistics and analytics
- ✅ Health monitoring

## API Endpoints

### Tasks
- `GET /tasks` - Get all tasks (with filtering & pagination)
- `GET /tasks/:id` - Get single task
- `POST /tasks` - Create new task
- `PUT /tasks/:id` - Update task
- `DELETE /tasks/:id` - Delete task

### Users
- `GET /users` - Get all users
- `POST /users` - Create user

### System
- `GET /health` - Health check
- `GET /stats` - Task statistics

echo "# TaskFlow - Task Management System" > README.md
echo "A full-stack task management application built with React.js and Node.js" >> README.md
echo "## Features" >> README.md
echo "- Create, read, update, and delete tasks" >> README.md
echo "- Task filtering and sorting" >> README.md
echo "- Statistics dashboard" >> README.md
echo "## Tech Stack" >> README.md
echo "- Frontend: React.js" >> README.md
echo "- Backend: Node.js, Express.js" >> README.md
echo "- Database: MySQL" >> README.md